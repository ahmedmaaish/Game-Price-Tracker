# Supabase Postgres connection string -- use the SESSION POOLER URI.
# Supabase Dashboard -> Connect -> Session pooler.
# (The "Direct connection" URI is IPv6-only and fails on GitHub's runners.)
SUPABASE_DB_URL=postgresql://postgres.PROJECTREF:[YOUR-PASSWORD]@aws-0-REGION.pooler.supabase.com:5432/postgres
